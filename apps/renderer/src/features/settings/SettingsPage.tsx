export function SettingsPage() {
  return (
    <div className="panel">
      <h1>Settings</h1>
      <p className="muted">
        Provider keys (keychain-backed), default org, and policy defaults will
        live here.
      </p>
    </div>
  );
}
